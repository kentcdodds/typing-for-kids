{
  "name": "typing-for-my-kid",
  "description": "A simple app I forked for my kid",
  "version": "0.0.51e",
  "dependencies": {
    "animate.css": "^3.7.2",
    "react": "^16.9.0",
    "react-dom": "^16.8.6"
  },
  "devDependencies": {
    "react-scripts": "^3.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && now build -n typing-for-my-kid"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
